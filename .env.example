# tsk runtime configuration
# Copy this file to `.env` and set only the values you need.

# Optional: custom local data directory (default: ~/.tsk)
# TSK_DATA_DIR=/absolute/path/to/.tsk

# Todoist OAuth app credentials
# TSK_TODOIST_CLIENT_ID=
# TSK_TODOIST_CLIENT_SECRET=

# Linear OAuth app credentials
# TSK_LINEAR_CLIENT_ID=
# TSK_LINEAR_CLIENT_SECRET=

# Asana OAuth app credentials
# TSK_ASANA_CLIENT_ID=
# TSK_ASANA_CLIENT_SECRET=

# GitHub OAuth app client id (optional when using `gh auth token` flow)
# TSK_GITHUB_CLIENT_ID=

# Optional: disable ANSI colors in output
# NO_COLOR=1

# Optional: editor used by `tsk config edit`
# EDITOR=vim
